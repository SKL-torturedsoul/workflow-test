spring:
  application:
    name: automation
 
management:
  endpoints:
    web:
      exposure:
        include: health,info, metrics, prometheus
    health:
      show-details: always    
   
camunda.bpm:
  admin-user:
    id: demo
    password: demo
    firstName: Demo
  filter:
    create: All task
 
